<%= form_for(@application_fee) do |f| %>
  <% if @application_fee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_fee.errors.count, "error") %> prohibited this application_fee from being saved:</h2>

      <ul>
      <% @application_fee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
